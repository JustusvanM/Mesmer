# Copy to .env and fill in real values. Never commit .env to git.
# Supabase (get from Project Settings → API)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key
# Use the service_role JWT from dashboard (starts with eyJ...) for server-side API routes
SUPABASE_SERVICE_ROLE_KEY=your_service_role_jwt_from_dashboard

# Stripe key encryption (min 32 chars). Generate: openssl rand -hex 32
STRIPE_ENCRYPTION_SECRET=your_32_char_minimum_encryption_secret

# Resend (contact form + waitlist emails) — get API key at resend.com
RESEND_API_KEY=re_your_resend_api_key
# Optional: verified sender e.g. Mesmer <notifications@gomesmer.com>
RESEND_FROM_EMAIL=Mesmer <onboarding@resend.dev>
# Where to receive waitlist signup notifications (default: justus@gomesmer.com)
WAITLIST_NOTIFY_EMAIL=justus@gomesmer.com
